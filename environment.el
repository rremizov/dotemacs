; (flycheck-set-checker-executable
;  'python-pylint
;  (concat (getenv "VIRTUAL_ENV") "/bin/pylint"))

;; (when (not (= (getenv "VIRTUAL_ENV"))))

; PATH=$(pwd)/pyenv/bin:$PATH
; VIRTUAL_ENV=$(pwd)/pyenv/
; PYTHONPATH=$(pwd)/server/src/:$PYTHONPATH
; emacs

